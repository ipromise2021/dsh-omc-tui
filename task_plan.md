# 任务计划：迁移为 Claude Code 风格的终端交互

## 目标

移除全屏备用缓冲的自绘交互，改为普通终端缓冲区的追加式 CLI，使 VS Code 原生负责滚轮、文本选择与复制，且保留 Harness 会话、命令和状态信息。

## 当前阶段

阶段 4：测试与验证

## 各阶段

### 阶段 1：需求与发现

- [x] 确认滚轮问题来自全屏 TUI 与终端输入协议冲突
- [x] 确定用户优先级：Claude Code 式简洁 CLI、原生选择、方向键历史
- [x] 将发现记录到 findings.md
- **状态：** complete

### 阶段 2：规划与结构

- [x] 划分追加式输出与临时输入区域的渲染边界
- [x] 保留需要全屏面板的功能或提供简化替代
- [x] 记录兼容性取舍
- **状态：** complete

### 阶段 3：实现

- [x] 移除主对话的备用屏幕依赖
- [x] 将全帧刷新迁移为普通缓冲区的增量输出
- [x] 将输入、状态与菜单变为局部临时区域
- [x] 保持方向键历史和 Harness durable events
- **状态：** complete

### 阶段 4：测试与验证

- [ ] 验证滚轮不会触发历史输入
- [ ] 验证原生文本选择和复制
- [x] 验证语法、ANSI 备用屏幕移除与 diff 一致性
- [ ] 验证会话、命令、面板与退出清理（需已安装 mock profile）
- **状态：** in_progress

### 阶段 5：交付

- [ ] 更新 README 的交互说明
- [ ] 汇总测试结果和已知限制
- **状态：** pending

## 已做决策

| 决策 | 理由 |
|------|------|
| 使用普通终端缓冲区而非备用屏幕 | 让 VS Code 自己处理滚轮、选择与复制，避免把滚轮解析成方向键。 |
| 保持键盘优先 | `↑/↓` 仍用于历史与菜单，不能与滚轮共享不可区分的转义序列。 |

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| VS Code 滚轮被解析为输入历史 | 3 | 停止通过终端模式修补，迁移渲染架构。 |

## 后续任务：TUI 插件市场调研与实现方案

### 目标

评估当前 TUI 是否已具备插件能力，以及是否能安全地在 TUI 中浏览并安装 DSH Hub 市场插件；先完成方案，不在本轮改代码。

### 结论

- 当前 TUI 是 Harness `dsh.bundle`，可消费 profile 中已安装的插件能力，但没有 catalog、插件安装服务或 `/plugins` 命令。
- 官方安装入口是 `dsh plugin --profile <profile> add/remove <source>`，由 pnpm 安装并根据 `dsh.bundle.patch` 重建 bundle 栈。
- 推荐 TUI 做市场前端和确认层，安装委托官方 CLI；安装后重启 TUI，不做当前进程热加载。
- DSH Hub 的事实、条目字段和安全约束详见 `findings.md`；分期路线详见 `ROADMAP.md`。

### 实施阶段（后续开发时执行）

1. [ ] 定义可替换 catalog adapter/schema，先接 mock JSON，支持缓存、超时和离线降级。
2. [ ] 实现 `/plugins` / `/market` 列表、搜索、筛选、详情和已安装视图（只读）。
3. [ ] 实现 host install broker，结构化调用官方 `dsh plugin`，不直接改 profile 文件。
4. [ ] 增加安装确认、来源/revision/许可证/构建风险展示、退出码与重启提示。
5. [ ] 在隔离 `DSH_HOME` 验证 npm、GitHub、子目录、固定 commit、卸载、失败恢复和 Windows 路径。

**本阶段状态：** research_complete；等待用户确认后再编码。
